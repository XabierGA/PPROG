#include <stdio.h>

main(){

    int r = 10;
    int c = 50;

    printf("%c[2J", 27);
    printf("%c[%d;%dH", 27, r, c);
    printf("Hello World");
    fflush(stdout);
    while(1);
}


image to ascii para interfaz

en resource.c nos falta la función display resource



IDEAS LOCAS

modo batalla, idiomas, mochila expandible, todo con probabilidades, niveles ( poder subir resource ) /*muy flipada*/

Versión demo, controladisima
para que funcione en la presentación